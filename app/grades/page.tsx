import { Award, Target, TrendingUp } from "lucide-react";
import { courses, grades } from "@/data/mock";

export default function GradesPage() {
  return <main className="page"><header className="page-header"><span className="eyebrow">ACADEMICS</span><h1>Grades</h1><p>Understand what you’ve secured and what remains.</p></header><div className="grade-summary"><section className="panel"><Award /><span>Current average</span><strong>74.3%</strong><small>Across 4 calculated courses</small></section><section className="panel"><Target /><span>Target average</span><strong>80%</strong><small>6.2 points to close</small></section><section className="panel"><TrendingUp /><span>Completed weight</span><strong>37.5%</strong><small>Semester assessment weight</small></section></div><div className="grade-grid">{grades.map((grade) => { const course = courses.find((item) => item.id === grade.courseId)!; return <section className="panel grade-card" key={grade.courseId}><div><span className="course-code">{course.code}</span><h2>{course.title}</h2></div><div className="grade-ring" style={{ "--grade": `${grade.current * 3.6}deg` } as React.CSSProperties}><span>{grade.current}%</span></div><div className="grade-stats"><p><span>Target</span><strong>{grade.target}%</strong></p><p><span>Completed</span><strong>{grade.completedWeight}% weight</strong></p><p><span>Remaining</span><strong>{100 - grade.completedWeight}% weight</strong></p></div><div className="progress-track"><i style={{ width: `${grade.current}%` }} /></div></section>; })}</div><p className="architecture-note">Grade calculations use a configurable scheme identifier; university-specific rules remain mock data in this milestone.</p></main>;
}
